const nextConfig = {
  images: {
    domains: ['aceternity.com', 'via.placeholder.com', 'images.unsplash.com', 'res.cloudinary.com'], // Add additional domains as needed
  },
};

export default nextConfig;
